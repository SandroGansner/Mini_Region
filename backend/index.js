import 'react-native-gesture-handler'; // Importiere react-native-gesture-handler zuerst
import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('mini_region_app', () => App);